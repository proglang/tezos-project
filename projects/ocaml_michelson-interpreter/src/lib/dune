(include_subdirs unqualified)

(library
  (public_name michelson)
  (name michelson)
  ; (modules absMichelson)
  (libraries base core ptime zarith ) ;tezos-crypto
  (preprocess (pps ppx_deriving.std )) ; ppx_jane -allow-unannotated-ignores
)
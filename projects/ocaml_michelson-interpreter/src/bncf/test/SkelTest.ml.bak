module SkelTest = struct

(* OCaml module generated by the BNF converter *)

open AbsTest

type result = string

let failure x = failwith "Undefined case." (* x discarded *)

let rec transInt (x : int) : result = match x with
    Int string -> failure x


and transNat (x : nat) : result = match x with
    Nat string -> failure x


and transProg (x : prog) : result = match x with
    Code (instr, instrs) -> failure x


and transInstr (x : instr) : result = match x with
    IPUSH (ctype, data) -> failure x
  | IADD  -> failure x


and transData (x : data) : result = match x with
    DInt int -> failure x


and transCType (x : cType) : result = match x with
    CInt  -> failure x



end

storage unit;
parameter (or (int %A)
            (or (int %B)
                (or (int %A_a)
                    (or (int %A_c)
                        (int %B_c)))));
code {
...
IF_LEFT { ... }                               (* %A *)
        { IF_LEFT { ... }                     (* %B *)
                  { IF_LEFT { ... }           (* %A_a *)
                            { IF_LEFT { ... } (* %A_c *)
                                      { ... } (* %B_c *)
}}}}}
